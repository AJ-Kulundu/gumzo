<% flash.each do |key, value| %>
    <div class="flex flex-col bg-gray-100 border-l-2 border-blue-500 text-blue-700 p-4 mb-4 max-w-xs justify-center" role="alert">
        <p class="font-bold"><%= key.to_s.capitalize %></p>
        <p><%= value %></p>
    </div>
<% end %>

