<nav class="flex items-center justify-between bg-gray-800 p-4">
    <div class="flex items-center">
        <span class="text-white text-lg font-semibold">Gumzo</span>
    </div>
    <div class="flex items-center">
    <%if user_signed_in?%>
        <%= button_to "Logout", session_path(current_user), method: :delete, data: { turbo_confirm:"Are you sure you want to logout?"} %>
    <%end%>
    </div>
</nav>
